---
title: "Example Title"
tags: example
---

This is an example. Here is how to embed inline bash

``` bash
echo hello world
```

ENDOFSUMMARY

^ that tag marks the end of the summary that will be shown on the main page.

## Include Source

Here's how to include a source file

<%= render_code("bash-script.sh", "bash") %>

For a long file, include only part of it with

<%= render_code("bash-script.sh", "bash", {:range => [1,2]}) %>
